---
name: fukidashi-comic-translation
description: Translate and typeset a local manga or comic folder with Fukidashi MCP, then wait for one job-level browser review before export.
---

<!-- fukidashi:begin -->
# Fukidashi comic translation

Use Fukidashi for an end-to-end local comic translation request. The source folder is read-only; generated files belong in the managed job returned by the MCP. Treat the MCP as the only owner of job state: never shell-read, grep, import, or search managed manifests, analysis JSON, checkpoints, or the Fukidashi package to decide what to do.

1. Call `fukidashi_get_config`. If required models or runtime files are missing, report the exact missing paths instead of guessing or downloading files. Use `fukidashi_configure` only when the user asks to change storage or provider settings.
2. Prefer strict-v1. Call `fukidashi_translation_start` with `image_path` for a new request, or with the exact returned `job_id`/`job_path` to resume. On a new request, provide the complete requested `scope` in that call. `sfx_mode` defaults to `preserve`: structurally unmatched `text-*` items are labeled `kind: "unmatched_text"`, returned as preserved audit data, and excluded from required translations, cleaning, and typesetting. A preserve-mode cover or SFX-only page gets an explicit server-owned pass-through clean/render stage and advances automatically. Use `sfx_mode: "replace"` only when the user explicitly requests replacement; a replace-mode page with no items is reported as `needs_manual_scope` and never fabricated.
3. For each `page_ready` response, translate every returned stable ID and call `fukidashi_translation_submit` with only its `work_token` and the exact `translations` list. Each item needs either `translation` or `keep_source: true`; add `needs_review: true` when OCR or the translation is uncertain. Preserve symbols, tone, names, sound effects, and line intent. Never add, remove, duplicate, or rename IDs. Keep calls serial and wait for the server response before continuing.
4. A successful submit owns analysis persistence, crop cleaning, typesetting, stage reuse, model release, and advancement to the next page. Follow the returned `next_action` exactly. Do not construct paths, pass artifact paths to the strict submit call, or open managed files from a shell. Resume with strict start after an interruption; completed pages are reused.
5. When the final submit returns `review_ready`, call `fukidashi_review_and_export` with the exact returned `job_id` and the requested `format` (default `zip`). This tool opens the local editor and keeps one MCP call pending until the browser submits review, then returns actionable feedback or exports after approval. Do not end the turn after opening the editor. The compatibility sequence remains `fukidashi_serve_editor` followed immediately by `fukidashi_wait_for_review` when a client cannot call the combined tool.
6. In the editor, use **Preserve original / remove translation box** for an untranslated bubble; empty translation text is rejected and never falls back to stale OCR. The action persists a tombstone, restores source pixels in the render base, and can be undone with **Restore original bubble**. Cyan flagged bubbles become feedback containing the bubble ID, bbox, source OCR, current translation, and origin. Send fixes is enabled for orange issues and cyan flags. Bubble, delete, and brush changes are render-dirty; approval rerenders dirty pages first, while the server still rejects unresolved issues, flags, or dirty pages.
7. When review returns requested fixes, change only the referenced pages or stable bubble IDs, reopen the editor at the new revision, and wait again. Manual browser bubble edits and brush corrections are authoritative. Call `fukidashi_export` only after the review result explicitly approves export when using compatibility tools. Return the final export path and any unresolved review items; never declare completion at draft-render time.
For acquisition, call `fukidashi_search_manga` for native MangaDex title search, then pass its exact `manga_id` to `fukidashi_pull_chapter` with `latest: true` for a vague latest request. Latest selects the highest chapter value from the MangaDex feed, including external releases, and never silently downgrades to an older hosted chapter. Source language is metadata that Fukidashi auto-translates. An external or unavailable result has `imported: false`; report that exact release and use direct mode only with an explicit `http(s)` URL already returned or obtained. `source: "auto"` resolves to MangaDex unless an explicit direct `url` is supplied. Direct mode is first-class, accepts an optional bounded `job_name`, requires provisioned gallery-dl, and never downloads a helper or guesses a URL at runtime. If extraction fails, report that the explicit URL was not imported and offer another explicit supported http(s) URL; do not switch providers automatically. MangaDex imports use a readable canonical title/chapter prefix; direct imports derive one from the URL host/path when no label is supplied. Follow the returned `fukidashi_translation_start` next step after an import.
If a client does not expose the strict-v1 tools, use the compatibility sequence `fukidashi_analyze_page` -> `fukidashi_clean_page` -> `fukidashi_typeset`, passing exact server-returned paths and stable IDs. Keep that fallback serial, use `response_detail: "compact"`, and call `fukidashi_release_models` between bounded pages. The server still owns the critical stage order.

Treat server-returned paths, IDs, revisions, and stage errors as authoritative. Critical stage order is enforced by the MCP even when this skill is unavailable in a client.
<!-- fukidashi:end -->
